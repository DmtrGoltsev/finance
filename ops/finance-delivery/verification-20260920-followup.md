# Проверка исправлений доставки, 20 сентября 2026

Ветка: `codex/investment-recommendations-delivery-20260920`.
Основа: `b165999f1779ba73efd7dbaa69552483e8c156e3`.
Push и развёртывание не выполнялись. Секреты не добавлялись.

## Исправленные замечания

1. Исчерпание попыток атомарно завершает ожидающее задание с `delivery_failed`.
   Авторизованный повтор и кнопка Android возвращают в очередь то же событие и
   задание без смены ключей. Конкурентные нажатия не сбрасывают аренду.
   Gateway сохраняет ключи защиты от дублей после очистки содержимого; поздний
   повтор не генерирует новый анализ. Сохранённый callback можно доставить повторно.
2. Downgrade `0024` нормализует `processing` в `pending` до удаления полей аренды.
   Идентичность события сохраняется при downgrade, upgrade и следующем захвате.
3. Контекст Docker закрыт разрешающим списком, COPY ограничен исходниками и
   манифестом. Секреты, окружения, Git и кеши исключены.

## Результаты

- Целевые backend-тесты: `111 passed`, 3 предупреждения (17,17 с).
  Команда: `python -m pytest tests/delivery tests/investments
  tests/api/test_accounts_categories_route_contract.py
  tests/api/test_openapi_mvp_manual_first_contract.py -q`.
- Полный backend: `409 passed, 6 skipped`, 15 предупреждений (61,97 с).
  Команда: `python -m pytest -q`. Пропуски существовали до исправления.
- Gateway: `npm test`, 31 тест, все успешны. Ответы внешних систем подставные.
  Проверяется паритет HMAC с реальным модулем backend через `BACKEND_HMAC_MODULE`.
- Android: `testDebugUnitTest`, `assembleDebug`, `compileDebugAndroidTestKotlin`
  успешно за 53 с; 198 unit-тестов в 29 наборах, без ошибок и пропусков.
  Включены контракт POST retry-delivery, сохранение состояния ошибки в кеше,
  повтор без нового UUID, сохранение профилей и ошибка сети.
- Ruff по изменённым Python-файлам: все проверки пройдены.
- `git diff --check`: успешно, ошибок пробелов нет.
- Alembic: один head `20260920_0024`; генерация PostgreSQL SQL для
  `0024→0023` и `0023→0024` успешна. Реальные функции миграции и повторный захват
  проверены в SQLite-регрессии; это не выполнение миграции на production.

Первый целевой прогон обнаружил лишний `default: null` в статическом OpenAPI;
контракт исправлен и повторный прогон успешен. Перед тестами gateway выполнен
`npm ci --ignore-scripts --no-audit --no-fund`, поскольку в worktree не было зависимостей.

## Непроверенное

Не запускались Docker, настоящий PostgreSQL, instrumentation, live n8n/FCM и
установка на телефон. Проверка Docker-контекста является статическим контрактом,
а не инспекцией образа. Проверки на PostgreSQL и gateway включены в существующий
`investment-delivery-ci.yml`, но удалённый CI не запускался: push запрещён задачей.
Для live FCM по-прежнему нужны внешние данные Firebase из README.
